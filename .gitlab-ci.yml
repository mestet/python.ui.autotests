image: docker:stable

default:
  tags:
    - docker
    - inside

stages:
  - test

run-test-job:
    stage: test
    image: python:3.10-slim
    script:
      - pip install -r requirements.txt
      - python3 -m pytest --alluredir=allure-result
